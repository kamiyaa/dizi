[server]
socket = "~/dizi-server-socket"

# Where to save playlist on exit
playlist = "~/dizi_playlist.m3u"

# supports alsa, jack
audio_system = "alsa"

# run a script whenever the song changes
# on_song_change = "some_script"

[server.player]

shuffle = false
repeat = true
next = true
volume = 50
